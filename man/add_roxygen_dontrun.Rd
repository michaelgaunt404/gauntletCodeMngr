% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_roxygen_dontrun.R
\name{add_roxygen_dontrun}
\alias{add_roxygen_dontrun}
\title{Ensure dontrun{} Wrapping Around @examples}
\usage{
add_roxygen_dontrun(folder = "R", test_individually = T)
}
\arguments{
\item{folder}{Character. The folder containing the R scripts to process. Defaults to "R".}

\item{test_individually}{Logical. If TRUE, prompts the user to press a key to continue
after processing each file. Defaults to TRUE.}
}
\description{
This function checks all R script files in a specified folder to ensure that
all \verb{@examples} sections in Roxygen documentation are wrapped in dontrun{}.
It ensures proper package building and simplifies function documentation
development for users.
}
\details{
The function performs the following checks on each script:
\itemize{
\item Ensures the script contains only one function.
\item Verifies that Roxygen documentation is present.
\item Checks if an \verb{@examples} section exists.
\item Ensures dontrun{} is not already present around the \verb{@examples} section.
}

If all conditions are met, the function automatically adds dontrun{} around the \verb{@examples} section
and relocates the \verb{@export} tag if it appears after the \verb{@examples} section.
}
\examples{
\dontrun{
# Assuming you have an "R" folder with R script files:
add_roxygen_dontrun(folder = "R", test_individually = TRUE)

# To process another folder:
add_roxygen_dontrun(folder = "path/to/your/folder", test_individually = FALSE)
}
}
