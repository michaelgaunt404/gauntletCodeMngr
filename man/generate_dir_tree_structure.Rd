% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_dir_tree_structure.R
\name{generate_dir_tree_structure}
\alias{generate_dir_tree_structure}
\title{Generate a Tree Representation of a Directory Structure}
\usage{
generate_dir_tree_structure(
  dir = ".",
  prefix = "",
  dirs_to_fully_list = c("R", "code", "data", "docs", "_targets", "analysis"),
  sample_n = 1
)
}
\arguments{
\item{dir}{Character. The directory path to start the tree structure. Defaults to the current directory \code{"."}.}

\item{prefix}{Character. A prefix string used for formatting subdirectory levels. Defaults to an empty string \code{""}.}

\item{dirs_to_fully_list}{Character vector. A vector of directory names that should be fully listed without sampling.
Defaults to \code{c("R", "code", "data", "docs", "_targets", "analysis")}.}

\item{sample_n}{Integer or NULL. The number of files to randomly sample per subdirectory if the directory is not
in \code{dirs_to_fully_list}. If \code{NULL}, all files are listed. Defaults to \code{1}.}
}
\value{
A character string representing the hierarchical structure of the directory.
}
\description{
\code{generate_tree_structure()} provides a basic textual representation of a file directory,
allowing users to view the layout of directories and files in a structured format.
}
\details{
This function scans a directory and recursively lists its files and subdirectories in a
structured tree format. Directories listed in \code{dirs_to_fully_list} are displayed completely,
while others may have files randomly sampled if \code{sample_n} is specified.
}
\examples{
\dontrun{
# Generate a tree structure for the current directory
generate_tree_structure()

# Generate a tree structure for a specific directory with no sampling
temp = generate_tree_structure(dir = "path/to/directory", sample_n = NULL)
message(temp)

# Generate a tree structure and sample up to 2 files per directory
generate_tree_structure(dir = "path/to/directory", sample_n = 2)
}
}
